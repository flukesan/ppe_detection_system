# Quick Start Guide

คู่มือเริ่มต้นใช้งาน PPE Detection System รวดเร็ว

## 🚀 วิธีการรันโปรแกรม

### Windows 🪟

**วิธีที่ 1: ใช้ Batch Script (แนะนำ)**
```cmd
run_windows.bat
```

**วิธีที่ 2: รันโดยตรง**
```cmd
python main.py
```

**วิธีที่ 3: ดับเบิลคลิก**
- ดับเบิลคลิกที่ไฟล์ `run_windows.bat`

---

### Linux/Ubuntu 🐧

**วิธีที่ 1: ใช้ Launcher Script (แนะนำ)** ✅
```bash
./run_ubuntu.sh
```

**วิธีที่ 2: รันโดยตรง** (อาจมี Qt/Wayland warning)
```bash
python3 main.py
```

**หมายเหตุ:**
- ครั้งแรกต้องให้สิทธิ์: `chmod +x run_ubuntu.sh`
- Script จะแก้ปัญหา Qt Wayland และ RTSP อัตโนมัติ

---

### macOS 🍎

```bash
python3 main.py
```

---

## ⚙️ โหมดการใช้งาน

### 1. Single Camera Mode (โหมดกล้องเดี่ยว)

**ตั้งค่าใน `config.yaml`:**
```yaml
multi_camera:
  enabled: false  # ปิดโหมด fusion
```

**วิธีใช้:**
1. รันโปรแกรม
2. คลิก `Camera` → `Connect Camera`
3. เลือกกล้อง (USB/RTSP/Video File)
4. คลิก `เริ่มตรวจจับ`

---

### 2. Multi-Camera Fusion Mode (โหมดกล้องหลายตัว) 🎥🎥

**ตั้งค่าใน `config.yaml`:**
```yaml
multi_camera:
  enabled: true  # เปิดโหมด fusion
  num_cameras: 2
  camera_sources:
    - 0  # USB Camera
    - "rtsp://user:pass@192.168.1.100:554/stream"  # RTSP Camera
```

**วิธีใช้:**
1. รันโปรแกรม
2. คลิก `Camera` → `Multi-Camera Fusion Mode` ✓
3. คลิก `Camera` → `Connect Camera`
4. คลิก `เริ่มตรวจจับ`

**หรือสลับโหมดผ่าน GUI:**
1. คลิก `Camera` → `Multi-Camera Fusion Mode` (checkbox)
2. ระบบจะสลับโหมดอัตโนมัติ

---

## 🎮 การควบคุมพื้นฐาน

### Keyboard Shortcuts

| Shortcut | คำสั่ง |
|----------|--------|
| `Ctrl+K` | เชื่อมต่อกล้อง |
| `Ctrl+O` | เปิดไฟล์วิดีโอ |
| `Ctrl+Q` | ออกจากโปรแกรม |

### เมนูหลัก

**File Menu:**
- `Open Video File` - เปิดไฟล์วิดีโอสำหรับตรวจจับ
- `Exit` - ออกจากโปรแกรม

**View Menu:**
- `Show Pose Keypoints` - แสดง/ซ่อน keypoints ของท่าทาง
- `Show FPS` - แสดง/ซ่อน FPS counter

**Camera Menu:**
- `Connect Camera` - เชื่อมต่อกล้อง
- `Multi-Camera Fusion Mode` - สลับโหมดกล้องหลายตัว
- `Disconnect` - ตัดการเชื่อมต่อกล้อง

**Settings Menu:**
- `Configure PPE Requirements` - ตั้งค่า PPE ที่ต้องการตรวจจับ

---

## 🎯 การตั้งค่าแบบด่วน

### กำหนด PPE ที่ต้องการตรวจจับ

1. คลิก `Settings` → `Configure PPE Requirements`
2. ไปที่แท็บ `🦺 อุปกรณ์ PPE`
3. เลือก PPE ที่ต้องการ:
   - ✅ **ตรวจจับ** - เลือก PPE ที่จะตรวจจับ
   - ✅ **บังคับ** - PPE ที่ต้องสวมใส่ (ขาดจะถือว่า Violation)
4. คลิก `นำไปใช้` หรือ `ตกลง`

### ตั้งค่าความไวการตรวจจับ

**แก้ไขใน `config.yaml`:**
```yaml
detection:
  confidence_threshold: 0.5  # เพิ่มค่า = ตรวจจับเข้มงวดขึ้น (0.0-1.0)
  nms_threshold: 0.4         # Non-Maximum Suppression
```

---

## 📹 ตั้งค่ากล้อง

### USB Camera

```yaml
camera:
  default_id: 0      # 0, 1, 2, ... (index ของกล้อง)
  width: 1280
  height: 720
  fps: 30
```

### RTSP Camera (IP Camera)

**ใน `config.yaml`:**
```yaml
camera_sources:
  - "rtsp://username:password@192.168.1.100:554/stream"
```

**รูปแบบ RTSP URL:**
```
rtsp://[username]:[password]@[ip]:[port]/[path]
```

**ตัวอย่าง:**
```
rtsp://admin:12345@192.168.1.100:554/stream1
rtsp://admin:wip50313@192.168.1.181:8554
```

### Video File

```yaml
camera_sources:
  - "/path/to/video.mp4"
```

หรือเปิดผ่าน GUI: `File` → `Open Video File`

---

## 🔧 Troubleshooting พื้นฐาน

### ปัญหา: ไม่เจอกล้อง

**Windows:**
```cmd
python -c "import cv2; print(cv2.VideoCapture(0).isOpened())"
```

**Linux:**
```bash
ls /dev/video*
v4l2-ctl --list-devices
```

### ปัญหา: RTSP ไม่เชื่อมต่อ

**ทดสอบ RTSP:**
```bash
# ใช้ ffplay
ffplay rtsp://admin:password@192.168.1.100:554/stream

# ตรวจสอบ network
ping 192.168.1.100
```

### ปัญหา: FPS ต่ำ

1. **ลด Resolution:**
   ```yaml
   width: 960
   height: 540
   ```

2. **ลด FPS:**
   ```yaml
   fps: 15
   ```

3. **ปิด GPU:**
   ```yaml
   detection:
     use_gpu: false
   ```

### ปัญหา: Qt Display Error (Ubuntu)

**วิธีแก้:**
```bash
export QT_QPA_PLATFORM=xcb
python3 main.py
```

หรือใช้ `./run_ubuntu.sh` (จะตั้งค่าให้อัตโนมัติ)

---

## 📊 ตัวอย่างการใช้งาน

### สถานการณ์ที่ 1: โรงงานขนาดเล็ก (1 กล้อง)

```yaml
multi_camera:
  enabled: false

detection:
  required_ppe:
    - helmet
    - vest
```

**การรัน:**
```bash
python main.py
# หรือ ./run_ubuntu.sh (บน Ubuntu)
```

### สถานการณ์ที่ 2: โรงงานขนาดกลาง (2 กล้อง)

```yaml
multi_camera:
  enabled: true
  camera_sources:
    - 0                                    # USB
    - "rtsp://admin:pass@192.168.1.100"   # IP Cam

fusion:
  strategy: "or"  # ตรวจจับอย่างละเอียด
```

**การรัน:**
```bash
./run_ubuntu.sh  # Ubuntu
# หรือ
run_windows.bat  # Windows
```

---

## 🎓 Next Steps

หลังจากเริ่มต้นใช้งานได้แล้ว:

1. **อ่านเอกสารเพิ่มเติม:**
   - [Multi-Camera Fusion Guide](MULTI_CAMERA_FUSION.md)
   - [Ubuntu RTSP Troubleshooting](UBUNTU_RTSP_TROUBLESHOOTING.md)
   - [Installation Guide](INSTALLATION.md)

2. **ปรับแต่งการตรวจจับ:**
   - กำหนด PPE ที่ต้องการ
   - Tune confidence threshold
   - เลือก fusion strategy

3. **Monitor Performance:**
   - ดู FPS counter
   - ตรวจสอบ GPU/CPU usage
   - ปรับ resolution/fps ตามต้องการ

---

## 💡 Tips

✅ **สำหรับผู้เริ่มต้น:**
- เริ่มด้วย single camera mode
- ใช้ USB camera ก่อน (ง่ายกว่า RTSP)
- ทดสอบด้วย video file ก่อน

✅ **สำหรับ Production:**
- ใช้ multi-camera fusion (ความแม่นยำ +20-40%)
- ตั้งค่า RTSP ด้วย TCP transport
- Monitor FPS และ compliance rate

✅ **เพิ่ม Performance:**
- ใช้ GPU (CUDA)
- ลด resolution ถ้า FPS ต่ำ
- ตั้ง `frame_skip` สำหรับ video file

---

**Version:** 1.0.0
**Updated:** 2025-12-10
**Platform:** Windows, Linux, macOS
